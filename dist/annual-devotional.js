(function () {
  "use strict";

  // Arquivo mantido apenas por compatibilidade com versões antigas instaladas.
  // O plano anual, as reflexões e a troca automática de cada dia agora são
  // controlados exclusivamente por app.js, evitando que um segundo script
  // sobrescreva o devocional já renderizado.
  window.DIARIO_DA_FE_ANNUAL_LEGACY = true;
})();
